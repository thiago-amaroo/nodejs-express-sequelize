//esse arquivo passa para o sequilize-cli, linha de comando do sequelize, onde estao as pastas que movi da raiz para dentro de src
//path é do node e gerencia caminhos
const path = require('path');

module.exports = {
    'config': path.resolve('./src/database/config', 'config.json'),
    'models-path': path.resolve('./src/database/models'),
    'seeders-path': path.resolve('./src/database/seeders'),
    'migrations-path': path.resolve('./src/database/migrations'),
}
